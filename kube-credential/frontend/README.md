# Frontend

Simple React (Vite) app with two tabs: Issue and Verify. It calls the backend services using base URLs configured via Vite env:

- `VITE_ISSUANCE_BASE_URL`
- `VITE_VERIFICATION_BASE_URL`

Dev:
```bash
npm install
npm run dev
```

Build:
```bash
npm run build
npm run preview
```
